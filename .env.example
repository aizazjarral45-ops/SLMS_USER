# Leave blank to use the browser-based local development data layer.
# Once the backend is available, set this to its API base path, for example:
# VITE_API_BASE_URL=https://api.example.edu/api/v1
VITE_API_BASE_URL=
